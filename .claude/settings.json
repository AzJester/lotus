{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && { [ -d node_modules ] || npm install; } >/tmp/lotus-setup.log 2>&1 || true"
          }
        ]
      }
    ]
  }
}
